[pytest]
addopts = -q --maxfail=1 --disable-warnings --html=reports/api-report.html --self-contained-html --junitxml=reports/junit/api-tests.xml
testpaths = tests
markers =
    smoke: Fast, critical-path tests
    negative: Negative tests (error cases)
    auth: Authentication/registration tests
    schema: JSON schema spot checks
